import { GradientChartDirective } from './gradient-chart.directive';

describe('GradientChartDirective', () => {
  it('should create an instance', () => {
    const directive = new GradientChartDirective();
    expect(directive).toBeTruthy();
  });
});
